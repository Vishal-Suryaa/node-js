<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Images</title>
</head>
<body>
    <h1>Images</h1>
    <ul>
        <% images.forEach(image => { %>
            <li>
                <img src="/uploads/<%= image.image %>" alt="<%= image.name %>">
                <span><%= image.image %></span>
            </li>
        <% }) %>
    </ul>
</body>
</html>